// Test null coalescing operator ??
// For bootstrap, ?? on non-optional just returns left operand
// Returns 42 if coalescing works correctly

fn main() i64 {
    const x: i64 = 42;
    const y: i64 = x ?? 0;
    return y;
}
