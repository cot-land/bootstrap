// Test multiple string interpolations
// expected: 42
fn main() int {
    var a = "hello";
    var b = "world";
    var s = "${a} ${b}!";

    // s should be "hello world!" (length 12)
    if (len(s) != 12) return 1;

    return 42;
}
