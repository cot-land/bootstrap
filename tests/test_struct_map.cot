// Test struct + map combination
struct Point {
    x: int,
    y: int,
}

fn make_point(x: int, y: int) Point {
    return Point{ .x = x, .y = y }
}

fn main() i64 {
    // Create struct from function
    var p = make_point(5, 6)
    if p.x != 5 { return 1 }

    // Create map
    var m = new Map<string, i64>()
    m.set("key", 100)
    if m.get("key") != 100 { return 2 }

    return 42
}
